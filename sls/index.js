(function() {
    'use strict';

    angular
        .module('sls', ['ui.router']);

        //
        
})();
















































/*
var APPLICATION_ID = '1C32EF32-4F17-A809-FFFE-1E36794D9700',
    SECRET_KEY = '66FEDE8D-9DBA-715F-FF55-1060B5CAA200',
    VERSION = 'v1'; //default application version;

Backendless.initApp(APPLICATION_ID, SECRET_KEY, VERSION);

var user = new Backendless.User();
user.email = "michael@backendless.com";
user.password = "my_super_password";
//Backendless.UserService.register(user);

function Comment(args) {
    args = args || {};
    this.message = args.message || "";
    this.authorEmail = args.authorEmail || "";
}

var dataStore = Backendless.Persistence.of( Comment );
var commentObject = new Comment( { message: "Hello, world!", authorEmail: user.email } );
dataStore.save( commentObject );
*/

